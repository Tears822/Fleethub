# Optional: worker-only overrides (loaded after repo root `fleethub/.env`).
# Per-tenant Uber sub-org (Vehicle Suppliers). Overrides baked-in map in uber-tenant-org-map.ts:
#   UBER_ORG_ID_TRADE_TAXI_SL=…
#   UBER_ORG_ID_COSCULLUELA=…
#   UBER_ORG_ID_TREVINO=…
# Legacy umbrella org (avoid for group tenant sync):
#   UBER_ORG_ID=…

# REDIS_URL=redis://localhost:6379
